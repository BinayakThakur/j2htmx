# j2htmx
A Java-first UI framework for Spring Boot and HTMX  J2HTMX allows developers to build complete web applications using Java components instead of HTML templates. Components generate HTML, integrate with HTMX, support fluent layouts, and work seamlessly with Spring Boot.
